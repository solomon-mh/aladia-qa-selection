describe("SubmitButton Component", () => {
	// Before each test, visit the page where the SubmitButton is located

	it("!passes", () => {
		cy.visit("http://localhost:3000");
	});
	// How can i pass other test cases
});
